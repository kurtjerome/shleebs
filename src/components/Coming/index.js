import React from 'react'
import { Wrapper, WorkInProgress, Description } from './style'

export default () => (
  <Wrapper>
    <WorkInProgress>Work in progress...</WorkInProgress>
    <Description>This part isn't ready yet. It's coming soon!</Description>
  </Wrapper>
)
